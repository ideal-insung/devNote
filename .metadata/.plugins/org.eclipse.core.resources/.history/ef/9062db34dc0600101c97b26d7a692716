package data_structure;

public class LinkedListGraph {

	MyNode head[];
	int vertexCnt;
	
	public void graphInit(Graph ghp, int size) {
		this.vertexCnt = size;
		int head[] = new LinkedListGraph();
		for(int i=0; i<size; i++) {
			this.head[i].data = i;
			this.head[i].cost = 1;
			this.head[i].next = null;
		}
	}
}
